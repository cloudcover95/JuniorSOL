On-chain Solana programs use different integers (u64 lamports, bytes).
Junior trit is {-1,0,1} on a *note*, packed I2_S hex in the handshake.

Do not send i2s_hex as a transaction payload.
CU receipt = Flagstaff + handshake. RPC off.

See JuniorLLM ports/sol_trit.py
